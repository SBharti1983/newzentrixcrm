{
  "name": "zentrixcrm-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*",
    "tools/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "npx tsx tests/unit/rohanHuman.test.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "vitest": "^3.2.0"
  },
  "overrides": {
    "vitest": "^3.2.0",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "opusscript": "^0.1.1"
  }
}
